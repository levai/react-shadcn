# AI-Friendly Frontend Template

这是一个专为 AI 辅助编程（Cursor, Copilot）优化的**企业级前端模板**，**完全适合大中型项目开发**。采用简化版 FSD (Feature-Sliced Design) 架构，集成了现代技术栈，旨在提供极佳的 DX（开发体验）和 AI 理解能力。

## 🌟 核心特性

- **架构**: 简化版 FSD (Feature-Sliced Design)
  - `app`: 全局布局、Provider、样式
  - `pages`: 页面组件（路由入口）
  - `features`: 业务功能模块（Auth 等）
  - `shared`: 通用 UI、Hooks、工具库
- **技术栈**:
  - React 19 + TypeScript
  - Vite 6 (构建工具)
  - Tailwind CSS v4 (样式引擎)
  - Shadcn UI (组件库基础)
  - Zustand (状态管理)
  - React Router v6 (数据路由)
  - Axios (网络请求)
  - react-i18next (国际化)
- **AI 友好**:
  - 包含 `docs/rules/ai-instructions.md` 统一 AI 助手规范（统一维护源）
  - 包含 `.cursor/rules` 开发规范（Cursor 专用）
  - 包含 `.github/copilot-instructions.md`（GitHub Copilot 专用）
  - 包含 `docs/workflows` 自动化工作流
  - 代码结构清晰，上下文明确
- **开箱即用**:
  - 完整的认证流程（Login, Protected Routes, Token Refresh）
  - 动态路由与侧边栏菜单生成
  - 深色/浅色主题切换
  - 国际化支持（中文/英文，可扩展）
  - 统一的 HTTP 客户端与错误处理

## 🚀 快速开始

### 安装依赖

项目支持使用 **npm**、**pnpm** 或 **yarn** 安装依赖：

```bash
# 使用 npm
npm install

# 使用 pnpm（推荐，速度更快、磁盘占用更少）
pnpm install

# 使用 yarn
yarn install
```

**注意：** 项目使用 **ahooks** 作为 Hooks 库，安装依赖时会自动安装。详细使用规范请查看 [Hooks 规范](./docs/rules/hooks.md)。

**注意：** 首次安装依赖后，Husky 会自动初始化。如果遇到问题，可以手动运行：

```bash
npm run prepare
# 或
pnpm prepare
# 或
yarn prepare
```

### 开发命令

```bash
# 启动开发服务器
npm run dev
# 或
pnpm dev
# 或
yarn dev

# 构建生产版本
npm run build
# 或
pnpm build
# 或
yarn build
```

## 📂 目录结构

```
src/
├── app/                 # 应用层
│   ├── layouts/         # 全局布局 (MainLayout, AuthLayout)
│   ├── globals.css      # 全局样式 & Tailwind 主题配置
│   ├── App.tsx          # 根组件
│   └── main.tsx         # 入口文件
├── features/            # 业务功能层
│   └── auth/            # 认证模块 (Store, API, Guards)
├── pages/               # 页面层 (路由组件)
│   ├── HomePage/
│   └── LoginPage/
├── routes/              # 路由层
│   ├── router.tsx       # 路由配置 (含 menuRoutes)
│   └── AppRouter.tsx    # 路由 Provider
└── shared/              # 共享层
    ├── api/             # HTTP 客户端
    ├── config/           # 环境变量配置
    ├── hooks/           # 共享 Hooks (useDebounce, useMediaQuery 等)
    ├── i18n/            # 国际化配置和语言资源
    ├── ui/              # UI 组件库 (Shadcn + Custom)
    ├── lib/             # 工具函数
    └── constants/       # 常量
```

## 🛠️ 开发指南

### 1. 添加新页面

1. 在 `src/pages` 下创建页面组件。
2. 在 `src/routes/router.tsx` 中添加路由配置。
   - 如果需要显示在侧边栏，添加到 `menuRoutes` 并配置 `meta`。

### 2. 添加 UI 组件

项目使用 Shadcn UI。

- 使用 CLI: `npx shadcn@latest add [component]`
- 或手动复制到 `src/shared/ui`。

### 3. 主题定制

修改 `src/app/globals.css` 中的 CSS 变量即可通过 Tailwind v4 自动应用。

## 📚 开发规范

**必须严格遵守以下规范：**

### 核心规范文档

- **[全局开发规范](./docs/rules/global.md)** - 技术栈、目录结构、命名规范、主题色规范
- **[UI 组件规范](./docs/rules/ui.md)** - Toast、主题、图标、表单、样式、**侧边栏开发规范**
- **[路由规范](./docs/rules/routing.md)** - 路由配置、路由保护、懒加载
- **[API 规范](./docs/rules/api.md)** - HTTP 客户端、错误处理、请求拦截
- **[Hooks 规范](./docs/rules/hooks.md)** - Hooks 库推荐、使用规范、集成方案
- **[国际化规范](./docs/rules/i18n.md)** - react-i18next 使用规范、语言资源管理

### 侧边栏开发规范（重要）

侧边栏开发**必须严格遵守**以下规则：

1. **结构顺序**：Logo → 导航菜单 → 收起按钮（禁止修改）
2. **样式规范**：
   - 必须使用渐变背景：`bg-gradient-to-b from-card/60 to-card/40`
   - Hover 效果统一使用：`hover:bg-card/80 hover:text-foreground`
   - 必须使用 shadcn/ui 标准类名，禁止自定义主题类名
3. **功能要求**：
   - 必须支持快捷键 Ctrl+B / Cmd+B
   - 必须实现拖动调整宽度（200-400px）
   - 折叠状态下必须显示 Tooltip
4. **禁止事项**：
   - ❌ 禁止使用自定义主题类名（如 `tactical-*`）
   - ❌ 禁止修改结构顺序

详细规范请查看：[UI 组件规范 - 侧边栏开发规范](./docs/rules/ui.md#侧边栏开发规范)

### 主题色规范

- 主色：`#05C79A`（青绿色）
- 必须同时支持浅色和深色模式
- 必须使用 CSS 变量，禁止硬编码颜色值

详细规范请查看：[全局开发规范 - 主题色规范](./docs/rules/global.md#主题色规范)

## 🔧 代码质量工具

### ESLint

项目使用 ESLint 进行代码检查：

```bash
# 检查代码
npm run lint

# 自动修复问题
npm run lint:fix
```

### Husky + lint-staged + commitlint

项目配置了 Git hooks，在提交代码前自动检查：

- **pre-commit**: 自动运行 lint-staged，检查并修复暂存的文件
- **commit-msg**: 使用 commitlint（官方标准工具）检查 commit message 格式

**Commit Message 格式规范：**

```
<type>: <subject>
```

- **type**: `feat`/`fix`/`docs`/`style`/`refactor`/`perf`/`test`/`build`/`ci`/`chore`/`revert`
- **subject**: 不超过 50 字，简明描述本次提交内容，使用中文

**示例：**
```
feat: 添加用户登录功能
fix: 修复侧边栏折叠问题
docs: 更新开发规范文档
```

**配置文件：** `commitlint.config.cjs` - 使用 Conventional Commits 规范

**详细规范：** 请查看 [Git 提交规范文档](./docs/rules/git.md)

**首次使用需要初始化 Husky：**

```bash
npm install
npm run prepare
```

## 🤖 AI 规范

请遵循 `.cursor/rules` 和 `docs/rules` 下定义的规范进行开发，确保代码风格一致性。

### 规范文档引用

**重要：** `.cursor/rules/*.mdc` 文件直接引用 `docs/rules/*.md` 文件。

- **只维护 `docs/rules/` 目录**，`.mdc` 文件会自动引用源文件
- 每个 `.mdc` 文件的 frontmatter 中包含 `source` 字段，指向对应的源文件

详细说明请查看：[.cursor/rules/README.md](./.cursor/rules/README.md)
